# Das Secret aus Teil 2, jetzt als Vorlage. stringData heisst weiterhin:
# Klartext rein, Kubernetes kodiert selbst nach base64.
apiVersion: v1
kind: Secret
metadata:
  name: {{ .Release.Name }}-secret
type: Opaque
stringData:
  APP_TOKEN: {{ .Values.appToken | quote }}

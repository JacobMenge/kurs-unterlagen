# MkDocs build output
site/

# Python
__pycache__/
*.py[cod]
*$py.class
venv/
.venv/
env/

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini

# Compose (Studierende erzeugen ihre eigene compose.yaml + .env)
apps/docker-compose-mission-control/compose.yaml
apps/docker-compose-mission-control/compose.*.yaml
apps/docker-compose-mission-control/.env

# Node-Artefakte falls jemand lokal probiert
apps/**/node_modules/

# Lokale Hilfs-/Arbeitsordner (nicht ins Repo)
.claude/

# Temporäre Arbeitsdateien (z. B. PDF-Textextraktion)
.tmp_*

# Folien-Werkzeuge (lokal installiert, nicht ins Repo)
folien/node_modules/
folien/package.json
folien/package-lock.json

# Erzeugte Foliensätze werden bewusst mitgeliefert – nur Zwischenstände nicht
folien/dist/*.tmp

# Interne Unterlagen gehören nach ../kurs-26.2b-orga und niemals hierher
*vertrag*
*honorar*
*Rahmenplan*
*rahmenlehrplan*
